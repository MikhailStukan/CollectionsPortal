@using Microsoft.AspNetCore.Mvc.Localization;

@inject IViewLocalizer Localizer
<div class="container-fluid">
    <h2>@Localizer["CollectionName"] @ViewBag.Collection.Name Owner: @ViewBag.Owner</h2>
    <h2>@Localizer["CollectionItems"] </h2>
    @{
        if(User.Identity.Name != null)
        {
           if(User.IsInRole("Administrator") || User.Identity.Name == ViewBag.Owner)
            {
            <button class="btn btn-success">@Localizer["AddItem"]</button>
            <button class="btn btn-danger">@Localizer["DeleteCollection"]</button>
            }
        }
        foreach(var item in ViewBag.Items)
        {
            <p>@item.Name</p>
        }         
    }
</div>